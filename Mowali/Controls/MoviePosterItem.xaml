<UserControl
    x:Class="Mowali.Controls.MoviePosterItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mowali.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="#ff222222">
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup x:Name="DisplayVisualStates">
				<VisualStateGroup.Transitions>
					<VisualTransition GeneratedDuration="0:0:0.2"/>
				</VisualStateGroup.Transitions>
				<VisualState x:Name="Default"/>
				<VisualState x:Name="LodingImage">
					<VisualState.Setters>
						<Setter Target="LoadingImageProgressRing.IsActive" Value="True"/>
					</VisualState.Setters>
					<Storyboard>
						<DoubleAnimation Storyboard.TargetName="LoadingImageGrid" Storyboard.TargetProperty="Opacity" To="1"/>
					</Storyboard>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
		<Image Source="{Binding PosterLocalPath}"/>

		<Grid x:Name="LoadingImageGrid" Opacity="0">
			<ProgressRing x:Name="LoadingImageProgressRing" VerticalAlignment="Center" HorizontalAlignment="Center" Height="100" Width="100" IsActive="False"/>
		</Grid>
	</Grid>
</UserControl>
